import React from 'react'
import { RelationSchema } from 'fusefx-modeldescription'

const RelationForm: React.FC<{ relation: RelationSchema }> = ({ relation }) => {
  return <div>RelationForm</div>
}

export default RelationForm
